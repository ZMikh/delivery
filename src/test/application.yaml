spring:
  datasource:
    url: jdbc:h2:mem:db
  jpa:
    open-in-view: false
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  kafka:
    consumer:
      bootstrap-servers: localhost:9099
    producer:
      bootstrap-servers: localhost:9099
